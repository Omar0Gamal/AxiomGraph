version: '3.8'

services:
  axiomgraph:
    build: .
    container_name: axiomgraph-server
    ports:
      - "8000:8000"
    volumes:
      - axiomgraph_data:/data
    restart: unless-stopped

volumes:
  axiomgraph_data:
